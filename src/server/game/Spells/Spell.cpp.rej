--- src/server/game/Spells/Spell.cpp
+++ src/server/game/Spells/Spell.cpp
@@ -2822,9 +2822,9 @@
             }
         }
     }
-    else if (m_spellInfo->AttributesCu & SPELL_ATTR0_CU_BINARY && unit->GetTypeId() == TYPEID_PLAYER && m_caster->GetTypeId() == TYPEID_PLAYER && !m_spellInfo->IsPositive(effectMask))
+    else if (m_spellInfo->IsBinary(effectMask) && unit->GetTypeId() == TYPEID_PLAYER && m_caster->GetTypeId() == TYPEID_PLAYER && !m_spellInfo->IsPositive(effectMask))
     {
-        m_resist = m_caster->CalcSpellResistance(unit, m_spellSchoolMask, m_spellInfo);
+        m_resist = m_caster->CalcSpellResistance(unit, m_spellSchoolMask, m_spellInfo, true);
         if (m_resist >= 100)
             return SPELL_MISS_RESIST;
     }
